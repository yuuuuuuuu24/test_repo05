[project]
name = "bonsai-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "huggingface-hub>=1.5.0",
    "cmake",
    "ninja",
    "setuptools",
]

[project.optional-dependencies]
webui = ["open-webui"]

[tool.setuptools]
packages = []
